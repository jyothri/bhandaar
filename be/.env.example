# Database Configuration
# These environment variables configure the PostgreSQL database connection

# Database host
# Default: hdd_db (Docker compose service name)
# For local development, use: localhost
DB_HOST=hdd_db

# Database port
# Default: 5432 (PostgreSQL default)
DB_PORT=5432

# Database user
# Default: hddb
DB_USER=hddb

# Database password
# IMPORTANT: Set this for production!
# Default: empty string (allowed for local development convenience)
# For production, use a strong password
DB_PASSWORD=hddb

# Database name
# Default: hdd_db
DB_NAME=hdd_db

# SSL mode
# Default: disable
# Options: disable, require, verify-ca, verify-full
# Use 'require' or higher for production deployments with remote databases
DB_SSL_MODE=disable
